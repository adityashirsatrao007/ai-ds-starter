name: Reusable Python Setup

on:
  workflow_call:
    inputs:
      python-version:
        required: false
        type: string
        default: "3.12"
      install-dvc:
        required: false
        type: boolean
        default: false
    secrets:
      WANDB_API_KEY:
        required: false
      HF_TOKEN:
        required: false

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: ${{ inputs.python-version }}
          cache: pip
          cache-dependency-path: "requirements.txt"
      - run: pip install --upgrade pip
      - run: pip install -r requirements.txt
      - if: ${{ inputs.install-dvc }}
        run: pip install dvc
      - if: ${{ secrets.WANDB_API_KEY != '' }}
        run: echo "WANDB_API_KEY configured"
        env:
          WANDB_API_KEY: ${{ secrets.WANDB_API_KEY }}
